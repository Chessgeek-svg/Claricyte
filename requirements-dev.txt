# Development-only dependencies. Not needed to run the demo.
-r requirements.txt

# The project itself, editable, so tests and scripts import claricyte from an
# installed package rather than relying on the repo root being on sys.path.
-e .

# scikit-learn is used solely by scripts/ingest.py, for the stratified
# train/val/test split of datasets that do not ship an official one.
scikit-learn==1.9.0

# Kept in step with the version CI pins in .github/workflows/ci.yml.
ruff==0.15.22
pytest==9.1.1

# The gold set is YAML so it stays readable in review. Eval-only: nothing the
# hosted demo runs reads it.
pyyaml==6.0.3

# Local generation backend, used by the eval loop so replaying the gold set
# dozens of times costs nothing. The hosted demo uses the OpenAI path instead.
# Must stay inside the range sentence-transformers asks for (>=5.0.0,<6.0.0 as
# of 6.0.0), or a clean install cannot resolve.
transformers==5.16.1
